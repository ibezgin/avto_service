extend type Query {
    role: RoleQuery!
}
extend type Mutation {
    role: RoleMutation!
}
type RoleQuery {
    allRoles: [RoleType!]!
}
type RoleMutation {
    addRole(title: String!, permission: JSON): Boolean
}

type RoleType {
    _id: String!
    title: String!
    permission: JSON
}
